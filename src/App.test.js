import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Didn't have time for these, sorry!
it('renders without crashing', () => {
  const div = document.createElement('div');
  ReactDOM.render(<App />, div);
});
